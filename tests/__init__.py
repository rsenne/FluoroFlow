"""FluoroFlow's test suite.

This file exists so that the test modules resolve as ``tests.*`` for both pytest
and mypy, which is what makes the per-module mypy override in ``pyproject.toml``
apply to them.
"""
